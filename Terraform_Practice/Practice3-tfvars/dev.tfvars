//  ami_id = "ami-0fa3fe0fa7920f68e"
//  instance_type = "t2.micro"


// if we want to dev.tfvars we need to add parameter when checking plan
// terraform plan -var-file="test.tfvars"
// if we dont put above parameter , nothing will call by default terraform.tfvar file.